language: c

compiler:
    - gcc

os:
    - linux

dist:
    - xenial

addons:
    apt:
        packages:
        - ninja-build
        - meson
        - libusb-1.0-0-dev
        - libsqlite3-dev

script:
    - make

