package(default_visibility = ["//visibility:public"])

exports_files(["javax.usb.properties"])

# Top-level package BUILD within java/ kept minimal: per-package BUILD files live under com/.
# This file intentionally contains no targets that compile sources; see per-package BUILD files.
