package(default_visibility = ["//visibility:public"]) 

# Minimal per-package BUILD for Kotlin sources in com.cowlark.fluxengine
# This package is deprecated: the project has been migrated to Java.
# No Kotlin compilation targets are provided here to avoid accidental builds.
